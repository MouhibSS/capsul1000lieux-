# Mapbox GL — required for the interactive map on LocationDetail pages.
# Get a free token at https://account.mapbox.com/access-tokens/
# Paste it into a .env file at the project root (never commit .env).
VITE_MAPBOX_TOKEN=pk.your_mapbox_token_here
